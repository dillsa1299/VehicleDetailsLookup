@page "/counter"
@inject HttpClient Http

<PageTitle>Counter</PageTitle>

<MudButton Color="Color.Primary" Variant="Variant.Filled" @onclick="TestAPI">Click me</MudButton>
@_testString

@code {
    string? _testString;

    private async Task TestAPI()
    {
        _testString = await Http.GetStringAsync("api/test/test") ?? "Nope";
    }
}
